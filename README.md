# Блог с комментариями - Тестовое задание Fullstack Developer

Веб-приложение для ведения блога с возможностью добавления статей и комментариев к ним.

## 📋 Описание проекта

Fullstack приложение, состоящее из:

- **Backend API** на Laravel для управления статьями и комментариями
- **Frontend** на React для отображения и взаимодействия с данными
- **Docker** для контейнеризации и упрощения развертывания

### Основной функционал:

- ✅ Просмотр списка статей с пагинацией (10 статей на страницу)
- ✅ Просмотр детальной информации о статье
- ✅ Создание новых статей
- ✅ Просмотр комментариев к статье
- ✅ Добавление комментариев к статье

---

## 🛠 Технологический стек

### Backend:

- **PHP** 8.2
- **Laravel** 12.x
- **MySQL** 8.4
- **Laravel Sail** (Docker окружение)
- **Nginx**

### Frontend:

- **React** 18
- **Vite** 7.x
- **React Router DOM** 6.x
- **Axios** для HTTP запросов
- **CSS** для стилизации

### Инфраструктура:

- **Docker** & **Docker Compose**
- **Redis** (кэширование)
- **Adminer** (веб-интерфейс для БД)
- **Mailpit** (тестовый почтовый сервер)

---

## 🚀 Установка и запуск

### Требования:

- **Docker** (версия 20.x или выше)
- **Docker Compose** (версия 2.x или выше)
- **Node.js** (версия 18.x или выше)
- **npm** (версия 9.x или выше)

---

## 📦 Backend (Laravel)

### 1. Перейдите в директорию backend:

```bash
cd backend
```

### 2. Создайте файл .env (если не существует):

```bash
cp .env.example .env
```

### 3. Запустите Docker контейнеры:

```bash
./vendor/bin/sail up -d
```

**Alias для удобства (опционально):**

```bash
# Добавьте в ~/.bashrc или ~/.zshrc:
alias sail='bash ./vendor/bin/sail'

# Затем используйте просто:
sail up -d
```

### 4. Установите зависимости Composer (если еще не установлены):

```bash
./vendor/bin/sail composer install
```

### 5. Сгенерируйте ключ приложения:

```bash
./vendor/bin/sail artisan key:generate
```

### 6. Выполните миграции и заполните БД тестовыми данными:

```bash
./vendor/bin/sail artisan migrate:fresh --seed
```

Эта команда:

- Пересоздаст все таблицы БД
- Создаст 15 тестовых статей через ArticleSeeder

## 🎨 Frontend (React)

### 1. Откройте новый терминал и перейдите в директорию frontend:

```bash
cd frontend
```

### 2. Установите зависимости:

```bash
npm install
```

### 3. Запустите dev server:

```bash
npm run dev
```

### 4. Откройте приложение в браузере:

```
http://127.0.0.1:5173
```

или

```
http://localhost:5173
```

---
